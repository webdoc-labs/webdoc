/**
 * Road represents a drivable 2D path for vehicles.
 *
 * @memberof R
 */
export class Road {

}

/**
 * A namespace dedicated to road manipulation utilites.
 *
 * @namespace
 */
const R = {
  /**
   * Builds a road
   *
   * @param {string} address
   * @return {Road}
   */
  buildRoad(address) {
    return null;
  },

  /**
   * The default no. of lanes per one side of road
   */
  DEFAULT_LANES: 4,
};
